# The deployment section should be excluded until
# it is explicitly included in module 5
deployment:
  dockerhub:
    branch: master
    commands: 
      - $DOCKER_HUB_TRIGGER

notify:
  webhooks:
    - url: https://hooks.slack.com/services/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

test: 
  override: 
    - mocha